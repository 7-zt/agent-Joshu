# reports/ —— 问题报告存储

inference-ops（`references/reporting.md`）的默认问题报告位置。命名：`YYYY-MM-DD-NN-theme.md`，NN 从 01 递增，theme 用短祈使句/短语/名词。

规则（与 reporting 参考一致）：

- 报告只含事实；定位与方案由后续独立任务产生，不回写进报告。
- 报告解决的问题：删除，或（若创建了后续任务）移入对应任务材料目录。
- 敏感信息（token、密钥、私有地址）脱敏后才能写入。
