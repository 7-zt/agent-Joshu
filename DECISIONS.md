# 工作流设计决定

本文件记录影响长期边界的设计决定。不是进度日志、发布说明或临时计划。

---

## 2026-09-17 | 中文单语言

**背景**：用户为单用户中文使用者。参考仓库 ruokee-agent-kit 使用英文主文档 + 中文审计副本的双语模式。

**决定**：本工作流使用中文单语言。frontmatter 标识与文件名用英文（兼容性），正文用中文。

**真实替代项**：
- 英文主文档 + 中文副本（ruokee 模式）
- 纯英文
- 完全中英对照（每段双语）

**后果**：
- ✓ 降低维护成本（不需双份文档同步）
- ✓ 用户阅读无障碍
- ✗ 公开发布需翻译
- ✗ 跨语言团队协作需额外工作

**重审条件**：公开发布或与英文团队长期协作时。

---

## 2026-09-17 | 组件自包含

**背景**：ruokee-agent-kit ADR 2026-08-24 要求组件自包含，避免安装时断链或依赖缺失。

**决定**：每个 Skill 目录必须自包含。禁止 Skill 内文件引用、书面路径引用、执行前置规则依赖组件目录外文件。必要规则复制最小版本进组件内。

**真实替代项**：
- 允许跨 Skill 引用（DRY 但脆弱）
- 中央共享库（复杂度更高）

**后果**：
- ✓ 任一 Skill 可单独复制、分发、安装
- ✓ 升级一个 Skill 不影响其他
- ✗ 某些规则（如环境采集命令）会在多个 Skill 重复

**重审条件**：出现大量规则重复导致维护负担时。

---

## 2026-09-17 | 版本知识审计链而非预生成

**背景**：推理引擎、CUDA、驱动的参数、兼容矩阵、行为会随版本快速变化。预生成知识易过期且产生幻觉。

**决定**：版本敏感知识不预生成。inference-stack 采用审计链模式：每条知识带 source/version/accessed/last_reviewed/status，由真实场景驱动填充，不批量预填。

**真实替代项**：
- 预生成知识库（如 40 篇引擎文档）
- 完全不建库，每次现场查询

**后果**：
- ✓ 知识不会过期被误用
- ✓ 每条知识可追溯来源
- ✗ 首次查询时库为空，需现场调研
- ✗ 需要纪律维护（定期复核 needs-review 条目）

**重审条件**：知识累积到一定量后评估维护成本。

---

## 2026-09-17 | OMP skills.customDirectories 加载

**背景**：OMP 18.2.3 支持通过配置加载自定义 Skill 目录。其他方式包括：全局安装、插件打包、内嵌到 OMP。

**决定**：使用 OMP `skills.customDirectories` 配置项，指向本仓库 `skills/` 目录（2026-09-21 仓库迁移后为 `C:/Users/admin/Desktop/yuting/program/agent-Joshu/skills`，原路径 `C:/Users/admin/Desktop/workspace/workflow/skills`）。

**真实替代项**：
- 打包为 OMP 插件（需构建流程）
- 全局安装到 OMP 默认目录（侵入性强）

**后果**：
- ✓ 开发与使用直接衔接，无安装步骤
- ✓ 修改立即生效（重启 OMP）
- ✗ 跨机器需手动同步目录
- ✗ 路径变化需更新配置

**重审条件**：需要跨机器分发或多人协作时。

---

## 2026-09-17 | reports/ 事实-only

**背景**：ruokee 的 bug_reports 只记录事实，禁止定位与方案。这防止过早收敛和认知偏差。

**决定**：本工作流的 `reports/` 目录采用相同规则。问题报告只记录现状/预期/环境/复现/已尝试，绝对禁止混入定位、根因、解决方案。

**真实替代项**：
- 允许报告包含分析（传统 bug report）
- 直接进任务，不单独建报告

**后果**：
- ✓ 报告客观、可被任意 Agent 独立消费
- ✓ 分析与事实分离，避免误导
- ✗ 需要克制不写分析的冲动

**重审条件**：无。这是核心防幻觉设计。

---

## 2026-09-17 | L0-L3 授权分级

**背景**：部署排障中某些命令破坏性强（重启服务、删除文件、改配置）。参考 ruokee 的授权边界设计。

**决定**：deploy-troubleshoot 使用 L0-L3 四级授权：
- L0（只读）：可直接执行
- L1（本地可逆）：说明意图后执行
- L2（改变环境）：用户确认后执行
- L3（影响运行系统）：必须明确授权 + 回退方法

用户请求「排查」不自动授权 L2/L3。

**真实替代项**：
- 二级（只读/写入）
- 每个命令都问（太繁琐）
- 全授权（不安全）

**后果**：
- ✓ 安全与效率平衡
- ✓ 风险分级清晰
- ✗ 需要 Agent 准确分级命令

**重审条件**：分级粒度不够或过细时。

---

## 2026-09-17 | 知识条目场景驱动

**背景**：inference-stack 规划了 40+ 条目。预生成会导致内容空泛、未验证、维护负担重。

**决定**：第一次出现真实场景（vLLM 启动失败、CUDA 兼容问题等）时，采集官方文档并按模板写入一个条目。不设数量目标，不批量填充。

**真实替代项**：
- 预生成全部条目
- 设置季度目标（如「Q1 补齐 10 条」）

**后果**：
- ✓ 每条知识都经真实需求验证
- ✓ 不浪费时间写不用的内容
- ✗ 初期库较空

**重审条件**：无。符合 YAGNI 原则。

---

## 2026-09-21 | 单 Skill 整合（inference-ops）

**背景**：初版按能力拆成 5 个 Skill（perf-analysis、deploy-troubleshoot、issue-report、tech-research、inference-stack）。实际使用中拆分过细：五个入口共享同一批纪律（证据、授权、版本、停止规则），维护成本高于收益，不符合个人 Agent Kit 的精简初衷。

**决定**：整合为单一 `inference-ops` Skill（部署排障 × 性能优化，报告/调研/知识沉淀作为输出形态），三模式（quick/full/consult）双轨（故障/性能）。19+1 篇 references 重组为 10 篇，方法论内容不删减。

**真实替代项**：
- 保持 5 Skill 拆分（入口语义更专，但纪律重复、加载面大）
- 合成 2 Skill（排障 + 性能；报告/调研归属仍尴尬）

**后果**：
- ✓ 单入口覆盖完整域；共享纪律只维护一份
- ✓ OMP 加载与触发更简单
- ✗ SKILL.md 模式路由变重；单目录丢失影响面变大（git 可恢复）

**重审条件**：域明显扩张（如训练侧运维）导致单 Skill 过大时。

---

---

## 2026-09-21 | 移植 ruokee-agent-kit 五个 Skill（英文正文例外 + 自动触发）

**背景**：用户要求把 ruokee-agent-kit（MIT）中除 msgspec 外的五个 Skill（grill-me、python-engineering、architect、code-quality、deep-research）移植到本仓库并支持自动触发。上游正文为英文，约 120 个文件。

**决定**：

- 五个 Skill 原样移植（正文不翻译），保留英文，便于与上游 diff 同步；作为 2026-09-17 中文单语言决定的显式例外。
- 除 inference-ops 维持用户触发外，五个移植 Skill 全部开放模型隐式调用：移除 `disable-model-invocation: true`，`agents/openai.yaml` 设 `allow_implicit_invocation: true`，并为缺失的三个 Skill 补建 openai.yaml。
- 同日调整：应用户要求，grill-me 恢复 `disable-model-invocation: true`（仅用户显式触发），避免模糊请求冷启动进入盘问式交互；其余自动触发不变。
- 同日追加：本地 codex 的 unslop 同规则移植（英文正文、开放隐式调用），上游声明「必须始终应用」，触发信号为撰写/改写任何面向用户的文本。
- 自动触发的路由信号与 Trellis 阶段映射写入 AGENTS.md「Skill 路由（自动触发）」节（Trellis 块外，`trellis update` 不会覆盖）。

**真实替代项**：

- 全量翻译为中文（符合单语言决定，但约 120 文件、译后有损且阻断上游同步）
- 保持上游的用户触发设置（不自动触发，违背本次移植目的）
- 修改 `.trellis/workflow.md` 做路由（Trellis 管理的文件，更新时可能被覆盖）

**后果**：

- ✓ 对话/Trellis 流程中按信号自动加载，无需 `/skill:` 显式调用
- ✓ 上游更新可直接 diff 合并
- ✗ 工作区存在中英双语言正文，需靠本条目与 AGENTS.md 例外说明约束
- ✗ grill-me 开放隐式调用后，模糊请求可能触发盘问式交互，需用户适应

**重审条件**：上游长期停更或用户阅读障碍明显时，重审是否翻译；自动触发噪声大时，可对个别 Skill 恢复 `disable-model-invocation: true`。

---

## 添加新决定的标准

只记录符合以下条件的决定：
- 影响长期边界（架构、兼容性、公共契约、开发流程）
- 有真实的替代方案（不是"做 vs 不做"）
- 后果会持续影响未来工作

不记录：
- 临时决定
- 无替代项的必然选择
- 已在 Skill 或 AGENTS.md 中明确的规则

## 格式

```markdown
---

## YYYY-MM-DD | 决定标题

**背景**：为何需要决定。

**决定**：具体决定内容。

**真实替代项**：
- 选项 A
- 选项 B
（无则写「无」，禁止编造）

**后果**：
- ✓ 好处
- ✗ 代价

**重审条件**：何时重新考虑此决定。

---
```
