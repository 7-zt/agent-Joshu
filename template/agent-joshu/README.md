# agent-joshu：本项目的 Agent 上下文

| 位置 | 内容 | git |
| --- | --- | --- |
| `adr/` | 架构决定记录（proposal/decision/archived/rejected，规则见 [adr/README.md](./adr/README.md)） | 进项目 git |
| `reports/` | 事实-only 问题报告（按需创建，命名 `YYYY-MM-DD-NN-theme.md`） | 进项目 git |
| `VERSION` | 本上下文来自主包模板的版本标记 | 进项目 git |

任务过程不放本目录的 git：memtrace 任务在项目根 `.memtrace/`（本地，gitignore）；规则见项目根 AGENTS.md 的 agent-joshu 节。安装与更新步骤见主包 `template/README.md`。
