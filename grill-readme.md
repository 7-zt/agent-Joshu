# grill 评审记录：README 快速开始细化

- 日期：2026-09-22
- 记录位置：根目录 grill-readme.md（用户选定）；交互方式：纯文本问答
- 主题：把 README「快速开始」从两行指针写得更详细
- 状态：**规格 v1.1（个人使用版）待确认**——v1.0 定稿后用户整体回退公开发布方向

## 基线（v1.1 当前值）

- 目标：README「快速开始」写得更详细，读者＝**未来的自己（换设备/隔久再装）**；仓库口径**维持个人使用，不做任何 ADR 反向**
- 形式：步骤＋预期结果＋链接（维持 v1.0 的 Q2 决定——该选择与读者定位无关）
- 覆盖：三条路径（新设备、新项目、首个 memtrace 任务体验），herdr 不进（维持 Q3）
- 范围：仅 README 快速开始一节；**ADR 零操作**（不反向、不加重审记录——公开发布未确立，chinese-monolingual 重审条件未命中）
- 非目标：ADR 动作（取消）、setup.md/template 重写、pyproject、翻译、Skill 语言、前言口吻调整

## 变更史（v1.0 → v1.1）

- **v1.0（已作废）**：公众读者＋公开发布＋中文单语＋ADR 一反一记。2026-09-22 定稿。
- **v1.1 修正（2026-09-22，用户原话「仔细想了一下，还是不要反向，保持个人使用，只是说快速开始应该更加详细一些」）**：
  - Q1 公众读者 → **推翻**，读者回到「未来的自己」（与 ADR 个人定位一致，冲突消解）
  - Q4 终值再修正：反向＋维持中文 → **不反向，保持个人使用**（ADR 全套动作取消）
  - Q5 ADR 打包（a 一件反向＋一件重审记录）→ **作废**
  - Q2 步骤＋预期结果＋链接、Q3 三条路径 herdr 不进：**维持**（读者中立）
  - 前置清单：由「公众读者从零清单」瘦身为「换设备提醒」（OMP 已装、Python 3.10+；不解释 git 等常识）
  - 已接受张力：README 前言对外口吻（「分享出来」+适用人群）与个人定位的口径差异**保留不处理**（用户知情，原盘点遗留项继续挂着）

## 事实与证据

1. 归档 ADR 2026-09-21-master-kit-and-project-contexts.md：「目标是个人可移植，不是公开发布」——**维持有效，不再反向**。
2. chinese-monolingual ADR（2026-09-17）：重审条件「公开发布时」——公开发布未确立，**未命中，无需动作**。
3. README 前言现为对外口吻；用户选择保留不动（张力挂账）。
4. memtrace v1 无 entry-point：真实调用 `PYTHONPATH=<主包根> python -m memtrace`；快速开始如实呈现。
5. omp/setup.md（新设备 3 步）、template/README.md（新项目 3 步）为安装细节单一真相。

## 规格 v1.1（实现依据）

### 交付物：README「快速开始」重写（中文，仅此一件）

1. **前置提醒**（一两行，面向换设备的自己）：OMP 已安装、Python 3.10+，细节见 omp/setup.md。
2. **路径 1 新设备**：克隆 → 配置 `skills.customDirectories`（关键命令＋预期：写入成功、重启生效）→ 跑 tests/omp-loading.md 触发矩阵（预期：8 Skill 按矩阵触发）；细节以 omp/setup.md 为准。
3. **路径 2 新项目**：复制 `template/agent-joshu/`＋主包 `.omp/extensions/memtrace/` → `memtrace init`＋并入 AGENTS.md＋gitignore（预期：`.agents/memtrace_config.toml`、`.memtrace/` 出现）→ 新 OMP 会话验证（预期：memtrace 注入出现、git status 只见预期文件）；细节以 template/README.md 为准。
4. **路径 3 首个 memtrace 任务体验**：create → log → read 三条可照抄命令（`PYTHONPATH=<主包根>` 形式）＋预期输出。
5. herdr 不进；不做双语；ADR 不动。

### 验收标准

1. 换设备时未来的自己只看快速开始即可走完两条安装路径（每步有预期结果，无需翻其他文档猜）。
2. 路径 3 三条命令照抄可执行，输出符合预期。
3. 快速开始不含 herdr；安装细节仍只在 setup.md/template/README.md 一处。
4. 仓库无任何 ADR 变更。

### 已接受风险与遗留

- README 前言对外口吻 vs 个人定位的张力保留（原盘点遗留项，未处理，无触发条件）。
- memtrace 裸命令缺失（PYTHONPATH 形式如实写）——遗留清单已有。

## 评审地图（v1.1 终态）

- 读者定位：未来的自己（Q1 经一次推翻后终值，与 ADR 一致）
- 展开形式：步骤＋预期结果＋链接（Q2，读者中立，维持）
- 覆盖范围：三条路径，herdr 不进（Q3，维持）
- 发布口径：个人使用，ADR 零操作（Q4 终值）
- ADR 打包：作废（Q5 作废）
- 前置提醒（瘦身版）／验证标准：操作默认，并入规格

## 偏好账本

- 2026-09-22：记录位置选根目录 grill-readme.md；交互选纯文本问答。
- 2026-09-22 Q1：先答公众读者；**v1.1 推翻：未来的自己**（动因：整体回退公开发布方向，与 ADR 对齐）。
- 2026-09-22 Q2：步骤＋预期结果＋链接（弃「命令全内联」）。
- 2026-09-22 Q3：1+2，herdr 不进。
- 2026-09-22 Q4：b 反向＋双语 → 回退为反向＋维持中文 → **终值：不反向，保持个人使用**（两次知情修正，均用户主动）。
- 2026-09-22 Q5：双语切分版作废；ADR 打包版作废。

## 问答记录

### 第 1 轮（2026-09-22）

**Q1（读者定位）** → 答：公共读者（后被推翻）。
**Q2（展开形式）** → 答：b 步骤＋预期结果＋链接。
**Q3（覆盖范围）** → 未答，重问。

### 第 2 轮（2026-09-22）

**Q3（重问）** → 答：1+2，3 不进。
**Q4（发布口径与语言）** → 答：b 反向＋双语（后被两次修正）。

### 第 3 轮（2026-09-22）

**Q5（双语切分）** → 用户要求展开；展开后修正 Q4＝放弃双语维持中文；Q5 作废。

### 第 4 轮（2026-09-22）

**Q5（新题·ADR 打包）** → 答：a 一件反向＋一件重审记录（后随公开发布整体回退而作废）。

### 第 5 轮（2026-09-22，收尾审计时）

无新问题——用户在共识审计确认阶段整体回退：**不反向、保持个人使用、快速开始仍要更详细**。效果：v1.0 作废，出 v1.1（读者＝未来的自己，ADR 零操作，Q2/Q3 维持）。

## 收尾与实施

- 2026-09-22：v1.1 经用户确认后实施。
- 实施：README「快速开始」重写为前置提醒＋三条路径（新设备安装/新项目接入/memtrace 30 秒体验），每步带「预期」；`read` 的预期描述按实测修正（回显 WAL 文件列表，看内容用 `--wal`）。
- 验收证据：①路径 3 三条命令照抄实跑通过（create/log 输出与 README 预期一致，read exit=0；初验的 OSError Errno 22 系验证管道 `head` 提前关管的人工伪影，去管道后干净）；②快速开始无 herdr（grep 证实）；③工作区仅 README.md 改动＋本记录，ADR 零变更；④安装细节单一真相仍为 omp/setup.md 与 template/README.md。
- 遗留挂账：README 前言对外口吻与个人定位张力（原盘点遗留项，未处理）；CLI 输出到提前关闭的管道时报 OSError（常规断管行为，未处理）。
